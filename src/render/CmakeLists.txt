# src/render/CMakeLists.txt

# Add a library for the input module
add_library(RenderTarget
    drawer.cpp
    drawer.h
    renderer.h)

# Include the current directory for header files
target_include_directories(RenderTarget PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})